server {
  listen 5173;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://api:4000;
    proxy_set_header Host $host;
  }

  location /uploads/ {
    proxy_pass http://api:4000;
  }

  location / {
    try_files $uri /index.html;
  }
}
